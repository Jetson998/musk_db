# Operations

One-off operational actions for musk_db.

Each subfolder `YYYY-MM-DD-short-name/` should contain a `README.md` with:
reason, affected files/services, implementation summary, verification plan,
rollback plan, deployment result, and backup paths/artifacts when applicable.

Template: `../docs/templates/minimal-release-record.md` (workflow Section 20).
